coverage:
    range: "80...100"

flags:
    nightly:
        joined: false

comment:
    show_carryforward_flags: true

github_checks:
    annotations: true

ignore:
  - "DOC.md" # Auto-generated doc
  - ".tools/**" # Used by CI (should be eventually exported somewhere else)
